export default {
	// namespaced: true,
	state: () => ({
      // shown: {
      //   logo: false,
      //   lang: false,
      //   lvl: false,
      // },
      
	}),

	getters: {
    // Shown_Main_Selection(state) {
		// 	const obj = state.shown;
		// 	const values = Object.values(obj);
		// 	return values.some((el) => el === true);
		// },
  },

	mutations: {
	},

	actions: {
    // showMainSettings({ dispatch }, name){
    //   dispatch("showSettings", {page: "main_page", name: name});
		// },
	},
};
